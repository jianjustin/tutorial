syntax = "proto3";

package add;

option go_package = "./proto;add";

service Add {
	rpc Add(AddRequest) returns (AddResponse) {}
}

message AddRequest {
	int64 a = 1;
	int64 b = 2;
}

message AddResponse {
	int64 result = 1;
}