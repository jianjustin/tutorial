syntax = "proto3";

package mul;

option go_package = "./proto;mul";

service Mul {
	rpc Mul(MulRequest) returns (MulResponse) {}
}

message MulRequest {
	int64 a = 1;
	int64 b = 2;
}

message MulResponse {
	int64 result = 1;
}